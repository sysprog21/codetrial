# Parity Gaps

> Historical record. The Python interviewer and the Next.js frontend it
> compares against are no longer in this repo; the rows below document how
> the Rust rewrite satisfied each contract, and the referenced goldens and
> scripts are the surviving evidence.

| status | current Python reference | `non-working` reference | frontend contract | required test | implementation direction | evidence |
|---|---|---|---|---|---|---|
| covered for Rust token route | The deleted Next.js token route minted `{ token, serverUrl, roomName }` with participant metadata `{ problemId, durationMin }`, clamped duration to 10-90 minutes, and granted room join, publish, subscribe, and data publish. | Git branch `non-working`, file `src/web.rs`, had a compatible static-server token shape. | The Rust `/api/token` route preserves the consumed response shape and fixed-room local override behavior. | `tests/web.rs` token API coverage | Keep the Rust token route compatible with the captured frontend contract. | `scripts/test.sh` runs Rust token route and token helper tests. |
| covered for recorded Python and frontend topic references | `tests/golden/` fixtures preserve the Python-reference behavior captured before deleting `agent/`; `src/runtime.rs` defines `code_update`, `control`, `test_results`, `report`, and `lk.transcription`; `src/agent.rs` consumes those frontend topics. | Git branch `non-working`, file `src/web/app.js`, uses `candidate_speech` and `agent_message` in addition to shared data topics. | The Rust-native browser UI keeps LiveKit voice assistant state and `lk.transcription`; do not switch to browser speech topics without a deliberate migration. | `tests/agent.rs`, `tests/runtime.rs`, and `tests/web.rs` | Preserve current topics first; treat browser speech topics as later UI migration work. | `scripts/test.sh` runs source-scoped Rust topic checks, typed payload fixtures, publish-path checks, and report runtime validation. |
| covered for recorded Python reference; deterministic Rust data ported; Python-derived fixtures frozen | `tests/golden/problems.json`, `tests/golden/prompts.json`, `tests/golden/timing.json`, `tests/golden/gemini-defaults.json`, `tests/golden/report-samples.json`, and `tests/golden/runtime-helpers.json` preserve deterministic Python-reference data captured before deleting `agent/`. | Git branch `non-working`, file `tests/golden/prompts.json`, adds browser-worker prompt text for candidate-requested editor reads that is not in the recorded Python reference. | The Rust-native browser UI expects spoken LiveKit agent behavior and report data on `report`. | `tests/agent.rs` and `tests/runtime.rs` | Keep deterministic Rust behavior aligned to recorded Python fixtures; treat the extra `non-working` editor-read wording as later runtime/UI migration behavior unless the browser contract changes. | Rust problem-bank, prompt, timing, config, helper, runtime, and report-helper tests assert Rust output matches committed fixtures, so test execution no longer imports or parses `agent/*.py`. |
| static wiring covered for Rust-native browser UI; credentialed Rust browser runner available | The deleted Next.js UI used `useVoiceAssistant`, `BarVisualizer`, and `registerTextStreamHandler(TOPIC_TRANSCRIPTION)` to drive status and transcripts. | Git branch `non-working`, file `src/web/app.js`, uses browser speech topics instead of LiveKit transcription for later UI work. | The Rust-native browser UI must keep LiveKit voice assistant state and `lk.transcription` text streams as the UI source until deliberate migration begins. | `tests/web.rs`; `BROWSER_CHECK_AGENT=rust ./scripts/browser-check.sh`; `scripts/parity-check.sh` | Preserve LiveKit voice/transcription UI behavior in the Rust agent rewrite; defer browser speech topics. | `scripts/test.sh` checks current voice assistant and transcript UI static wiring. `scripts/browser-check.sh` has opt-in credentialed Rust room checks and `scripts/parity-check.sh` compares Rust output to recorded Python reference fields. |
| closed: browser speech not adopted | The Rust runner now preserves recorded Python-reference LiveKit voice/transcription behavior through native audio, LiveKit agent state, and `lk.transcription`. | Git branch `non-working`, file `src/web/app.js`, uses browser `SpeechRecognition`, `speechSynthesis`, `candidate_speech`, and `agent_message`. | Do not run two competing speech stacks in the same UI. The Rust-native web solution keeps the LiveKit contract because it supports native Gemini audio, room-backed agent state, barge-in, and transcription parity. | `tests/web.rs`; `tests/agent.rs`; `BROWSER_CHECK_AGENT=rust ./scripts/browser-check.sh` | Keep browser-speech topics out of the default browser UI unless a future product decision intentionally replaces LiveKit voice behavior. | Browser-speech fallback coverage was not imported because the browser-speech path was not adopted. Home and interview-room visuals were migrated while preserving LiveKit topics and controls. |
| Rust static server smoke covered; browser parity has executable room-backed Rust checks | `tests/golden/visual/` preserves the final Next.js `/` and `/interview` screenshots captured before deleting the tracked `frontend/` app. | Git branch `non-working`, file `src/web/index.html`, was used only as earlier static-web evidence. | The Rust static web solution must preserve the captured frontend behavior and visual contract. | `scripts/server-check.sh`; `scripts/visual-parity-check.sh`; `scripts/browser-check.sh` with `BROWSER_CHECK_AGENT=rust` when LiveKit and Gemini credentials are present. | Keep server smoke cheap by default; use visual goldens and credentialed Rust browser checks against recorded Python reference fields for opt-in browser validation. | `scripts/test.sh` starts `agent web`, checks static home markup, checks fail-closed token behavior, and runs visual parity only when `CODETRIAL_BROWSER=1`. |
| covered for static-web migration evidence | The final Next.js visual references are committed in `tests/golden/visual/`; tracked `frontend/` source has been removed. | Git branch `non-working` serves a dark static app from `src/web/`, with browser speech topics and a Rust web server that starts a local interviewer per room. | Future UI work should compare against committed goldens rather than resurrecting the deleted app. | `docs/non-working-evidence.md`; `tests/golden/visual/` | Use `non-working` only as historical visual/static-web evidence unless a future task deliberately restores it as a reference. | Captured `non-working` commit `41ebbde` home visuals and recorded source-read behavior notes on 2026-08-12. Final Next.js visual goldens were captured before deleting tracked `frontend/`. |
| Python-reference and Rust room-backed browser voice, transcript, test-run, and scored-report checks covered; Rust checks isolate the local runner | `tests/golden/browser-python.json` and `tests/golden/report-python.json` preserve stable Python browser reference fields captured before deleting `agent/`. Rust now owns the room-backed LiveKit/Gemini runtime. | Git branch `non-working`, Rust server shape is useful as process reference but does not preserve current LiveKit voice/transcription behavior. | The Rust-native browser UI expects a real LiveKit voice assistant participant, `lk.transcription` streams, and report data on `report`; no browser-speech fallback topics yet. Rust-mode evidence must not be satisfied by a hosted LiveKit agent that auto-joins the same room. | `scripts/parity-check.sh`; `scripts/report-parity-check.sh`; `BROWSER_CHECK_AGENT=rust ./scripts/browser-check.sh` | Keep Rust visible to `useVoiceAssistant`; in Rust-mode browser checks, remove non-local agent participants before browser assertions and recheck the room before writing capture evidence. Compare Rust browser results against recorded Python reference fields so normal parity checks no longer launch the deleted Python runner. | `./scripts/parity-check.sh` uses `tests/golden/browser-python.json` for the recorded Python problem/transcript reference and runs only the isolated Rust browser flow. `./scripts/report-parity-check.sh` uses `tests/golden/report-python.json` for recorded Python problem/test-run text and requires a non-fallback Rust report shape after Run tests and End interview. On 2026-08-12, isolated Rust voice and barge browser checks passed with `rustAgentParticipants` containing only the local `interviewer-interview-*` participant. |
| covered for Rust local run docs | The Python local run path used a LiveKit Agents worker that joined new rooms automatically. | Git branch `non-working` serves a Rust-backed static web app later. | Current local docs must let the Rust runner and browser join the same LiveKit room without changing the default random room contract. | `tests/web.rs` token API coverage | Use optional `INTERVIEW_ROOM_NAME` only for local Rust runs; default token minting remains random. | README local run commands use `INTERVIEW_ROOM_NAME=interview-local` with `cargo run -- serve`; token coverage checks fixed-room override and default random rooms. |
| covered for Rust runner metadata | Python waits for the candidate participant and reads problem/duration from participant metadata before starting the session. | Git branch `non-working` does not preserve the current LiveKit participant-metadata contract. | The Rust token route and static interview URL can select problem/duration; the Rust runner must use the candidate metadata instead of local CLI values. | `src/livekit.rs` unit coverage; `scripts/browser-check.sh` Rust flow token metadata assertion | Derive `run-livekit ROOM_NAME` problem and duration from the first standard remote participant metadata before greeting. | Rust waits for a standard remote participant, parses metadata with the Python-compatible helper, then opens Gemini with that problem/duration. The Rust browser check asserts the runner logs `problem=two-sum duration=20min`; README no longer documents problem/duration CLI args. |
| Gemini Live setup contract covered; real-room smoke covered by Rust browser checks | Recorded Python-reference fixtures preserve the previous model/voice defaults and prompt/runtime contracts. | Git branch `non-working`, Rust shape can inform raw event loops but cannot replace the current LiveKit voice contract. | Rust must configure Gemini for native audio, current voice/model, tool declarations, input/output transcription, interruption, context compression, session resumption, realtime media messages, tool responses, parsed server events, state-backed tool execution, `lk.transcription` routing, subscribed LiveKit audio/video forwarding, Gemini audio output, LiveKit voice-assistant state, periodic prompt delivery, and final report publication before real-room validation. | `src/gemini.rs` and `src/livekit.rs` unit coverage plus `scripts/gemini-check.sh`; `scripts/parity-check.sh`; `scripts/report-parity-check.sh` | Smoke the Rust runner against the Rust static web UI. | `cargo test --lib` covers setup JSON, endpoint encoding, setup-complete parsing, redaction, mock handshake, realtime message shapes, tool response shape, server event parsing, Rust `read_editor`/`log_hint` execution, Gemini output transcript options for the browser `lk.transcription` handler, Gemini input transcript routing with candidate sender identity, little-endian PCM byte conversion for LiveKit-to-Gemini audio input, JPEG-throttled LiveKit-to-Gemini video input, Gemini-to-LiveKit audio frame assembly, `lk.agent.state` attribute publication helpers, Gemini report request configuration, report prompt state, reliable `report` publication helpers, data-event prompt delivery, runtime watch prompt gates for silence nudges plus proactive review, and initial greeting delivery from the Rust bootstrap; historical local smoke evidence from 2026-08-12: `scripts/gemini-check.sh` returned `setupComplete`; Rust browser parity/report checks now run against the Rust static web UI. |
| covered for Rust output scheduling and isolated barge-in smoke | LiveKit Agents runs voice playout, interruption, and input forwarding inside the SDK session. | Git branch `non-working` uses browser speech events and is not a LiveKit voice parity reference. | Candidate speech during a long Rust agent answer must reach Gemini quickly enough to trigger interruption, and queued Gemini output must stop when Gemini reports interruption. | `BROWSER_CHECK_AGENT=rust BROWSER_CHECK_FLOW=barge ./scripts/browser-check.sh` | Keep output playout off the main event loop and keep the room-backed barge-in smoke isolated from hosted LiveKit agents. | `OutputAudio::interrupt()` clears the LiveKit native output source buffer and the Rust partial PCM buffer. `OutputAudio::capture()` queues cancellation-tagged frames for a bounded background worker, so awaiting `NativeAudioSource::capture_frame()` no longer blocks the main `tokio::select!` loop from polling candidate audio. Rust tracks a projected playout deadline so LiveKit agent state and idle prompt gates remain busy until queued output should finish. `cargo test --lib output_audio` covers queued-frame cancellation tagging, projected playout deadlines, and interruption cancellation state. On 2026-08-12, isolated `BROWSER_CHECK_AGENT=rust BROWSER_CHECK_FLOW=barge BROWSER_CHECK_CAPTURE=/tmp/rust-barge.json ./scripts/browser-check.sh` passed with one candidate segment, `alexAfterCandidate=true`, and `rustAgentParticipants` containing only the local `interviewer-interview-*` participant. |
